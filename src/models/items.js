/*const { conn } = require('../config/conn');

const getAll =()=> {
    const [rows] =  conn.query('SELECT * FROM product;'); // no me manda limpio el json me trae row y fields
    return rows;
}

module.exports = {
    getAll
}*/